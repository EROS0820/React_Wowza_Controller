import { makeStyles } from "@material-ui/styles";

export default makeStyles(theme => ({
    root: {
        width: '100%',
        padding: '0 2vw',
        display: 'inline-flex',
        alignItems: 'center',
    },
    grade: {
        marginLeft: 'auto'
    },
}));
